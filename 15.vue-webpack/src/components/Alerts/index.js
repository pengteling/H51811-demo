import Alerts from './Alerts'

// export default {
//   install(Vue, options) {
//     Vue.component(Alerts.name, Alerts)
//   },
// }
export default function (Vue, options) {
  Vue.component(Alerts.name, Alerts)
}
